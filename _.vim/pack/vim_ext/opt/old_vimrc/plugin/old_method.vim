"" Thi options are usually too old to delete
set backspace=indent,eol,start
    " allow backspacing over everything in insert mode,
    " gvim need this...
set confirm

if has('mouse')
    "set mouse=a
    set mouse=""
endif

set hidden      " A buffer becomes hidden when it is abandoned
